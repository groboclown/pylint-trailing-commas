"""Test function arguments."""


def one_arg(
        _abc
):
    """One argument function, one value, so no required comma."""


def two_arg(
        _abc, _efg,
):
    """Two argument function, on separated line.  End comma necessary."""
